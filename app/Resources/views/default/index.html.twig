{% extends 'base.html.twig' %}

{% block body %}
	<div>
		{% for product in products %}
			<div>
				<h1>{{product.name}}</h1>
            <img src={{product.image}} />
			</div>
		{% endfor %}
	</div>
{% endblock %}
